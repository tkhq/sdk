---
"@turnkey/core": minor
"@turnkey/react-native-wallet-kit": minor
"@turnkey/react-wallet-kit": minor
---

- Added `overrideApiKeyStamper` and `overridePasskeyStamper` methods to allow updating the stamper configurations during runtime
- Added `deleteApiKeyPair` method for cleaning up key pairs that aren't associated with active sessions
