---
"@turnkey/core": minor
"@turnkey/react-wallet-kit": minor
---

Added mobile WalletConnect support with deep linking to wallet apps, filtered by chain namespace.

Exposes new `fetchWalletConnectApps` and `buildWalletConnectAppEntries` helpers (plus `WalletConnectApp` / `WalletConnectAppEntry` types) from `@turnkey/core`.