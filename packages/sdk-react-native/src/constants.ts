// default storage key
export const TURNKEY_DEFAULT_SESSION_STORAGE = "turnkey-session";

// fixed storage keys (used internally by the SDK)
export const TURNKEY_EMBEDDED_KEY_STORAGE = "turnkey-embedded-key";
export const TURNKEY_SESSION_KEYS_INDEX = "turnkey-session_keys-index";
export const TURNKEY_SELECTED_SESSION = "turnkey-selected-session";

export const OTP_AUTH_DEFAULT_EXPIRATION_SECONDS = 15 * 60;
