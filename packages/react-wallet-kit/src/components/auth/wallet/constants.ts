export const BUFFER_SIZE = 50; // Number of items to render outside visible area
export const WALLET_BUTTON_HEIGHT = 56; // Height of each wallet button in pixels.
export const ROW_GAP_PX = 8; // matches Tailwind `gap-2` on the wallet list flex container
export const ROW_STRIDE = WALLET_BUTTON_HEIGHT + ROW_GAP_PX;
export const INITIAL_VISIBLE_ROWS = 20; // pre-mount this many rows before first scroll
