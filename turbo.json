{
  "$schema": "https://turborepo.dev/schema.json",
  "tasks": {
    "build": {
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "dependsOn": ["^clean", "^build"],
      "outputs": [
        "build/**",
        ".vercel/**",
        "dist/**",
        ".next/**",
        "!.next/cache/**"
      ]
    },
    "clean": {},
    "test": {
      "dependsOn": ["^build"],
      "cache": false
    },
    "typecheck": {
      "inputs": [
        "$TURBO_DEFAULT$",
        "tsconfig.json",
        "!dist",
        "!build",
        "!.next",
        "!.vercel",
        "!**/*.md"
      ]
    },
    "dev": {
      "persistent": true,
      "cache": false,
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "dependsOn": ["^build"],
      "outputs": [
        "build/**",
        ".vercel/**",
        "dist/**",
        ".next/**",
        "!.next/cache/**"
      ]
    }
  }
}
