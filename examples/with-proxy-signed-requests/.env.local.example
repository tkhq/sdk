# Parent org API keys (server-side only — never expose to client)
API_PUBLIC_KEY=
API_PRIVATE_KEY=

# Parent org ID (used both server-side and client-side)
NEXT_PUBLIC_ORGANIZATION_ID=

# Turnkey API base URL
NEXT_PUBLIC_BASE_URL=https://api.turnkey.com
