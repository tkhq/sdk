{
  "name": "@turnkey/example-payflow-demo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "run-setup": "tsx src/scripts/setup.ts",
    "run-demo": "tsx src/scripts/demo.ts",
    "clean": "rimraf ./dist ./.cache",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@turnkey/sdk-server": "workspace:*",
    "@turnkey/crypto": "workspace:*",
    "@turnkey/ethers": "workspace:*",
    "dotenv": "^16.0.3",
    "ethers": "^6.10.0",
    "prompts": "^2.4.2"
  },
  "devDependencies": {
    "@types/prompts": "^2.4.2",
    "typescript": "5.4.3"
  }
}
