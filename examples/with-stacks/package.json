{
  "name": "@turnkey/with-stacks",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "pnpm -w run build-all",
    "start": "tsx src/index.ts",
    "clean": "rimraf ./dist ./.cache",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@stacks/common": "^7.0.2",
    "@stacks/transactions": "^7.1.0",
    "@turnkey/sdk-server": "workspace:*",
    "dotenv": "^16.0.3",
    "path": "^0.12.7"
  }
}
